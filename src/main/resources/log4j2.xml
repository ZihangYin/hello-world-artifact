<?xml version="1.0" encoding="UTF-8"?>

<configuration>
	<properties>
		<property name="name">Application</property>
		<property name="pattern">%d{ISO8601}{GMT+0} [%-5.5p] (%t) %C(line %L) - %m%n</property>
	</properties>
	<appenders>
		<RollingFile name="RollingFile" fileName="logs/${name}.log"
				 filePattern="logs/$${date:yyyy-MM}/${name}-%d{yyyy-MM-dd}-%i.log.gz">
			<PatternLayout>
				<pattern>${pattern}</pattern>
			</PatternLayout>
			<Policies>
				<TimeBasedTriggeringPolicy /><!-- Rotated everyday -->
				<SizeBasedTriggeringPolicy size="100 MB"/> <!-- Or every 100 MB -->
			</Policies>
		</RollingFile>
	</appenders>
	<loggers>
		<root level="DEBUG">
			<appender-ref ref="RollingFile"/> 
		</root>
	</loggers>
</configuration>